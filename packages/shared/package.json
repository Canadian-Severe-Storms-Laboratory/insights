{
    "name": "@insights/shared",
    "type": "module",
    "private": true,
    "exports": {
        "./utils": "./src/utils/index.ts",
        "./db": "./src/db/index.ts",
        "./queue": "./src/queue/index.ts"
    },
    "scripts": {
        "drizzle:generate": "bun run --env-file=../../.env drizzle-kit generate",
        "drizzle:push": "bun run --env-file=../../.env drizzle-kit push",
        "drizzle:migrate": "bun run --env-file=../../.env drizzle-kit migrate",
        "lint": "oxlint -c ../../.oxlintrc.json 'src/**/*.ts' 'src/**/*.tsx'"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "drizzle-kit": "^0.31.7",
        "tsc-alias": "^1.8.16"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "bullmq": "^5.64.1",
        "dotenv": "^17.2.3",
        "drizzle-orm": "^0.44.7",
        "ioredis": "^5.8.2"
    }
}
